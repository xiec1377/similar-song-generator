### Song similarity generator

Frontend:

To run locally 
```
npm run dev
```

Backend:

To run locally
```
python -m venv venv #create virtual env
venv\Scripts\activate # on windows
source venv/bin/activate # on macos and linux
uvicorn app.main:app --reload

```